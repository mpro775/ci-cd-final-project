apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pipeline-pvc
spec:
  storageClassName: skills-network-learner
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
